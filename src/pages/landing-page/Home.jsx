import { HeroSection, SetupProcess } from "./sections";

const Home = () => {
  return (
    <section className="bg-dark">
      <HeroSection />
      <SetupProcess />
    </section>
  );
};

export default Home;
